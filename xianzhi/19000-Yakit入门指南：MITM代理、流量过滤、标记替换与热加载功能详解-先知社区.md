# Yakit入门指南：MITM代理、流量过滤、标记替换与热加载功能详解-先知社区

> **来源**: https://xz.aliyun.com/news/19000  
> **文章ID**: 19000

---

## 1、下载安装

访问<https://yaklang.com/>直接下载

![e51fc87e-5539-497a-b165-a29a39f6f210.png](images/20250925110935-1085ba18-99bd-1.png)

安装好，启动即可

![8c1ea1f2-1b1d-49d6-bd91-d56b151b4342.png](images/20250925110935-10e1cd58-99bd-1.png)

## 2、MITM中间人代理与劫持

### 免配置启动

* 使用免配置模式。不需要任何其他配置，只需安装Chrome 浏览器，免配置启动之后将打开一个新的浏览器会话，其中所有流量都将通过 Yakit 自动代理。无需安装 Yakit 的 CA 证书就可以使用它来通过 HTTPS 进行测试。

点击`手工测试`--`MITM交互式劫持`即可进入MITM劫持页面，。

![3fb99e43-6051-46a2-8540-bf69b77cacee.png](images/20250925110936-111feaf0-99bd-1.png)

点击`免配置启动`进入免配置启动设置界面。根据

![eefeb5bc-1bcb-4d0a-87d6-65db0ca9874d.png](images/20250925110936-1143e32e-99bd-1.png)

进入之后，输入目标网址即可

![9b4a4545-075d-44f6-9b19-c675320cb0be.png](images/20250925110936-11659968-99bd-1.png)

免配置模式下，未安装证书也可以直接抓到https流量

![7b846fa5-9060-4280-b69e-a3c399786a73.png](images/20250925110937-11aaee86-99bd-1.png)

### 证书安装

不采用CA证书的情况下需要安装正护士

进入高级配置下载证书，下载之后的证书后缀是pem，手动重命名成crt即可。

![image.png](images/20250925110937-11f67fca-99bd-1.png)

双击安装证书

![image.png](images/20250925110937-1218be14-99bd-1.png)

## 3、实用小配置

### 暗黑模式

暗黑模式公测时间：2025年8月1日

官方地址链接：<https://mp.weixin.qq.com/s/pTV8DBEeYJYjjq22dyJIYA>

设置 → 模式切换→ 开启**「暗色」**→ 立即生效，无需重启

![image.png](images/20250925110938-124443cc-99bd-1.png)

### 流量过滤

在抓包过程中，会出现很多影响测试的杂包流量，Yakit提供了筛选过滤器功能。针对某些数据包我们不需要看见，或者屏蔽某些污染数据就可以输入需要过滤的条件。如下图：

![image.png](images/20250925110938-12a67b1e-99bd-1.png)

在劫持页面点击`过滤器`，将弹出筛选过滤器界面。

```
按照过滤条件的不同，过滤器划分出5个子选项，分别是

按照Hostname过滤 你可以在包含Hostname 输入想要仅显示的hostname，来筛选只想要看到的相关Hostname的请求。也可以在排除Hostname中输入不想要看到的Hostname进行排除。

按照URL路径过滤 你可以在包含URL路径输入想要URL路径来匹配相关的请求，也可以在排除URL路径中输入不想要展示的URL来过滤不想要看到的请求。

按照文件类型过滤 通过文件类型在过滤消息列表，这里有两个选择可供操作。一是仅仅显示哪些，另一个是不显示哪些。如果是仅仅显示哪些，在包含文件后缀的输入框中填写显示的文件类型，同样，如果不显示哪些文件类型，只要在排除文件后缀的输入框中填写不需要显示的文件类型即可。

按照Content-Type过滤 此过滤器的功能是指，根据HTTP请求头的Content-Type来排除不想要展示的相关数据包。

按照HTTP 方法过滤 此过滤器可根据HTTP请求方法进行的排除来达到过滤的目的
```

以hostname为例

![image.png](images/20250925110939-12e986ca-99bd-1.png)

再度抓包，就不会有其他hostname的流量

![image.png](images/20250925110939-13166442-99bd-1.png)

## 4、标记与替换功能

#### 标记

标记功能主要是用于对测试过程中发现的某些可用信息进行标记，以免后续查找利用。

如下图，将接口标记为蓝色

![image.png](images/20250925110940-13580c12-99bd-1.png)

##### Hae规则导入

在日常测试当中，Burp当中的HAE插件对于测试人员来说，极大提升了测试效率。但是Yakit似乎并没有相关的插件。于是，经过查找手册，发现了类似的功能。

产品文档地址：<https://yaklang.com/en/products/mitm/Mark-Replace-Traffic/#%E5%AF%BC%E5%85%A5%E5%AF%BC%E5%87%BA%E8%A7%84%E5%88%99>

在劫持页面的内容规则选项我们可以看到默认配置、导入配置、导出配置 三个与内容规则相关的操作选项。

点击默认配置

![image.png](images/20250925110940-138fe9f4-99bd-1.png)

在弹出的对话框中，点击 ①`使用默认配置`，导入JSON以后，点击 ②`导入`。

成功导入官方默认的55条规则内容，如果想使用其他规则，只需将JSON文件复制进来，点击导入即可

![image.png](images/20250925110940-13b065a6-99bd-1.png)

那么如何将HAE的规则导入呢。这里借助github上面大师傅提供的脚本

地址：<https://github.com/youmulijiang/HaeToYakit>

运行脚本转换即可

![image.png](images/20250925110940-13c3c2b8-99bd-1.png)

再导入yakit当中

![image.png](images/20250925110940-13e0d146-99bd-1.png)

导入成功后，可通过导出配置来分享给他人

![image.png](images/20250925110941-13fb6790-99bd-1.png)

导出为json文件

![image.png](images/20250925110941-141211c0-99bd-1.png)

成功标记颜色

![image.png](images/20250925110941-142da6ec-99bd-1.png)

#### 替换

在规则内容页点击右上角新增规则，然后对每个选项进行配置。

![image.png](images/20250925110941-145c6afe-99bd-1.png)

替换成功

![image.png](images/20250925110942-14b68e30-99bd-1.png)

## 5、爆破

使用过Burp Suite的同学都知道，它有一个Intruder模块，它的核心其实并不是某一个功能，而是一整套的操作流：

```
劫持数据包
发送到Intruder
清除占位符，添加新的占位符
为占位符设置一个值序列，如字典，多行字符串，一串数字等
设置爆破模式，开始爆破
```

Yakit 使用了一种全新的方法，**可以直接在Web fuzzer模块中完成重放与爆破两种工作**，更轻松地实现批量发包模糊测试。

#### 模糊测试标签

首先，我们需要介绍模糊测试标签(fuzztag)。fuzztag是一种小型语法结构，它大概长这个样子：`{{tagname(tagvalue)}}`，例如`{{randint(1,100)}}`生成1-100随机整数。

我们可以看到实际上fuzztag非常像一个函数调用，只是外层包裹了两个花括号。以`randint`这个fuzztag为例，其实所有fuzztag都只接收一个字符串，但是会在后端处理时会特殊处理。

下面简单介绍两种方法

##### 直接插入fuzz tags

直接插入fuzz tags就是在需要爆破的位置输入标签即可。如下图，爆破1-10之间的数字，标签为{{int(1-10)}}

![image.png](images/20250925110942-15076a12-99bd-1.png)

发送请求后，即可完成爆破。这一点在实际测试数据量等功能，小生认为还是很使用

![image.png](images/20250925110943-155916be-99bd-1.png)

##### 右键插入fuzz tags

与之相对的，Web Fuzzer也提供了更简单的方式来插入fuzztag，我们只需要在Web Fuzzer中右键选择**插入标签/字典**即可：

![image.png](images/20250925110943-1591d8c8-99bd-1.png)

###### 插入空字节标签：{{hexd(00)}}[#](https://yaklang.com/products/Web%20Fuzzer/fuzz#%E6%8F%92%E5%85%A5%E7%A9%BA%E5%AD%97%E8%8A%82%E6%A0%87%E7%AD%BEhexd00)

`{{hexd(00)}}`将在所在位置插入一个空字节，可以用于测试空字节截断漏洞。这实际上也体现了fuzztag的另外一个好处，我们不需要Burp Suite的十六进制编辑器，也可以轻松地插入十六进制数据。

![image.png](images/20250925110944-15dfcb78-99bd-1.png)

###### 插入字典标签

插入字典，这一点跟burp差别不大，都是加载本地字典。可能唯一的区别在于这里直接右键插入即可

![image.png](images/20250925110944-161d8474-99bd-1.png)

插入字典后

![image.png](images/20250925110945-164aaacc-99bd-1.png)

发送请求后，即可进行爆破。

![image.png](images/20250925110945-1693414c-99bd-1.png)

###### 插入加密标签

组合标签即可，{{base64enc({{int(1-10)}})}}，个人感觉似乎有点考验眼力.....

![image.png](images/20250925110946-16e598e8-99bd-1.png)

值得注意的是，官方的文档似乎有点过时了，没有跟随版本更新而更新。如下图当中的{{base64(randint(100))}}已经无法使用了

![image.png](images/20250925110946-171ea892-99bd-1.png)

使用{{base64(randint(100))}}异常

![image.png](images/20250925110946-176d22cc-99bd-1.png)

想要了解fuzztag可用标签和使用样例的同学可以查看这篇文章：[Fuzz Tag Playbook](https://yaklang.com/docs/yakexamples/fuzztag)。

## 6、热加载

热加载是一种允许在不停止或重启应用程序的情况下，动态加载或更新特定组件或模块的功能,

yakit官方对于热加载定义解释是：在Web Fuzzer 和用户之间，允许写一段 Yak 函数，在 Web Fuzzer 过程中使用，从而实现自定义 fuzztag 或更多功能。

### 调用热加载 fuzztag

调用热加载中编写的函数其实与平常的fuzztag类似，格式为：

```
{{yak(funcname)}}
```

当需要传入参数时，则格式为

```
{{yak(funcname|param)}}
```

函数标准形式：

```
// 函数名为funcname，参数只有一个，为param，类型是字符串
funcname = func(param) {
    // 返回值可以是字符串或数组
    return param
}
```

### 第一个热加载函数

通过艰难的翻阅文档，总算写出了第一个加密函数。

函数名：basetest

参数：test

```

basetest = func(test) {
    test = codec.EncodeBase64(test)

    return test
}
```

![image.png](images/20250925110947-1797d2e4-99bd-1.png)

实际操作一下：没问题过关

![image.png](images/20250925110947-17c06bb4-99bd-1.png)

后续的热加载魔术方法后面有时间再研究，官方提供的地址有些已经过时了，感觉还是有点晦涩难懂，可能是笔者水平还不够。

## 总结

```
yakit大体来说还是不错的，但是可能是开发者想要将burp类似的功能集中在一个界面上实现，
所以目前使用起来有点繁琐，同时有部分手册内容随着yakit更新而过时，不利于初学者上手。
但是在加载大量js文件时，似乎不会像burp一样卡死，这一点感觉很好。
```
